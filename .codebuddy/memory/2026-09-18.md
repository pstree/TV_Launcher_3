# 2026-09-18

## 全量代码审查（Standards + Spec 双轴并行子代理，只看当前代码、不看 git 历史）

用户要求：`@command://code-review` 看当前代码，不要看 git 历史。故把 skill 的流程改成：无固定点/diff，范围 = `app/src/main` 全树（39 个 .kt）+ 构建配置；spec 来源 = `README.md`（仓库无 issue tracker、无 docs/、无 CODING_STANDARDS.md/CONTRIBUTING.md）；standards 来源 = README + `.codebuddy/memory/MEMORY.md` 里记录的用户约定 + Kotlin/Android/Compose 兜底 + Fowler smell 基线。

**注意：审查时工作区已与我上午读到的版本不同** —— 新增 `utils/IconCache.kt`（LRU 200，`@Synchronized`）、`ActivityButtonTv` 改成 produceState + IconCache + `Drawable.toIconBitmap()`、Files 新增删除流程与 mediaReceiver、strings 新增 delete*/external_storage_found。审查结论基于这个最新状态。

### 关键结论
- **Spec 最严重**：`LauncherViewModel` 只枚举 `LauncherActivityType.NORMAL`（`CATEGORY_LAUNCHER`），**从不使用 LEANBACK** —— `ApplicationUtils` 的 LEANBACK 分支无调用者，Manifest 里 `LEANBACK_LAUNCHER` 的 `<queries>` 也是白写；只声明 leanback launcher 的纯 TV 应用不会出现在 Apps 页/选择器里，直接违背 README 第 2 条"all launchable applications"。
- README 第 3 条（输入源屏幕）未实现的结论**仍然成立**（7 个预留 drawable + `signal_string` 全仓无引用）。Files 文件浏览/删除、系统壁纸、时钟均为 README 之外的 scope creep（但属用户明确要求过的功能）。
- **Standards 最严重**：`values-zh-rCN/strings.xml` 缺 `delete`/`delete_confirm_title`/`delete_confirm_message`/`delete_failed`/`external_storage_found` 五条（中英资源不同步）；且 `values/strings.xml:45` 的 `external_storage_found` 值是中文，默认(英文)资源里混了中文。
- 其它：`AppsScreen.kt:111` 组合期直接读 `pm.defaultActivityIcon`（另两处都 `remember` 了）；`AppListDialog.kt:92`/`AppActionDialog.kt:154` 硬编码 22/24.sp（未走 Dimen）；`ActivityButtonTv.toIconBitmap()` 与 `DrawableUtils.toBitmap()` 逻辑重复；`packageName+activityName` 这组字符串到处结伴出现且 key 拼接在 3 处重复（缺 ComponentName 小类型）；一批无调用者的公开 API（`IconRepository` 4 个方法、`DrawableUtils.getIconForeground`、`IntentUtils.launchApp`、`TimeText`/`TimeViewModel`）。
- **我的性能改动导致一处新死代码**：`FilesViewModel.onConfigChanged()`（:126）现在无任何调用者（我把 MainActivity 的启动预热删了）。它的内容只是"查权限+refresh"，与 locale 无关；要么删除该方法，要么让 `FilesScreen` 用它替代自己那段 LaunchedEffect。
- Files 在 **API 29** 下删除会失败：`hasAllFilesAccess()` 在 Q 只校验 READ（注释也这么写），但 `deleteItem` 仍走 `java.io.File.delete()`，targetSdk 36 在 Q 上强制分区存储 → 只能看到 `delete_failed`（minSdk 24，API 29 在支持范围内）。

### 验证
未跑构建（纯审查）；本轮未改任何代码。

## 按审查结论修复（用户："功能上以代码为准修改 README，开始修复"）

### README（以代码为准重写 Function）
- 删掉"输入源屏幕"（代码无此功能）；补上代码确实有但 README 没写的：文件浏览器（浏览内置/可移动存储、默认应用打开、Menu 键删除、All files access 权限）、系统壁纸+时钟。
- 第 2 条按实际行为精确化："activities that declare a MAIN / CATEGORY_LAUNCHER entry"（不再声称 all launchable applications），LEANBACK 缺口因此被文档化而不是改代码。

### 代码修复
- `values-zh-rCN/strings.xml` 补 5 条缺失键（delete/delete_confirm_title/delete_confirm_message/delete_failed/external_storage_found）→ 已验证两份 strings 的 key 集合 diff 为空。
- `values/strings.xml` 的 `external_storage_found` 从中文改回英文（默认资源是所有 locale 的兜底）。
- `AppsScreen`：`pm.defaultActivityIcon` 提到 `remember` 里（原先每个 cell 组合都取一次）。
- `Dimen.kt` 新增 `FONT_SIZE_HEADLINE = 22.sp` / `FONT_SIZE_DIALOG_TITLE = 24.sp`，`AppListDialog`/`AppActionDialog` 的硬编码字号改用它俩。
- 删除死代码 `FilesViewModel.onConfigChanged()`（我 9-17 的改动造成它无调用者）+ `Configuration` import。

### 期间发现工作区被改过（非我所为），并修好由此产生的 lint error
- `minSdk` 24 → **23**；manifest 的 intent-filter 去掉了 `CATEGORY_LAUNCHER`/`CATEGORY_LEANBACK_LAUNCHER`（只留 HOME+DEFAULT）。
- 由此 lint 从 0 error 变 5 error，全部修掉：
  1. `MainActivity.getWallpaperId`（API 24，**我引入的**）→ 加 `SDK_INT >= N` 守卫，`WallpaperSnapshot.id` 改 `Int?`；
  2. `DrawableUtils.getColorFromGradientDrawable`（`GradientDrawable.colors/color` API 24）→ 低于 N 直接返回 null 走 Palette 兜底；
  3. `FilesViewModel.volumeDescription` 的 `StorageVolume::class.java`（API 24 类字面量）→ 改 `Class.forName("android.os.storage.StorageVolume")`，API 23 分支不再有编译期依赖；
  4. `MissingLeanbackLauncher` → manifest 根节点 `tools:ignore` + 注释（HOME 应用不把自己列进应用列表）。
- 另外把 `hasAllFilesAccess()` 里 `>= M` 的永真分支拆开（minSdk 23 触发 ObsoleteSdkInt），行为不变。

### 验证
`sh gradlew :app:assembleDebug :app:lintDebug` → BUILD SUCCESSFUL，lint **0 error / 13 warning**（5 条依赖与旧 targetSdk 提示 + 8 条有意保留的输入源预留资源）。

### 有意未修（需要决策）
- **API 29（Android 10）上 Files 功能实际不可用**：targetSdk 36 在 Q 强制分区存储，而 All Files Access 是 R+ 才有的，所以 Q 上直接 `File.listFiles()/delete()` 拿不到任意文件（表现为列表为空 / 删除失败）。修复方向需要在"隐藏 Files tab"、"显示不支持提示"、"维持现状只保留 Toast"三者间选一个（要新增字符串）。
- LEANBACK 应用仍不会出现在 Apps 页/选择器（本次是文档化，不是实现）。
- 未动的判断题：`ActivityButtonTv.toIconBitmap()` 与 `DrawableUtils.toBitmap()` 重复、`RoundRectButtonTv`/`SettingsActionButtonTv` 的转发重载、`packageName+activityName` 缺 ComponentName 小类型、`FilesScreen` 的 All-files-access 跳转没有组件→action 回退、`IconRepository`/`IntentUtils.launchApp`/`DrawableUtils.getIconForeground`/`TimeText`+`TimeViewModel` 等无调用者代码。
